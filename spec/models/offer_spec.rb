require 'spec_helper'

describe Offer do
  
	# it "should be higher than the current highest offer"

	it "should make its post inactive after an offer 
		on the post is accepted" do

		# ugh I don't know what I'm doing. Commenting this all out -Alex
			
			# post = Post.new
			# userCreatingPost = User.new(first_name: "Bob", last_name: "Smith")
			# userOffering = User.new(first_name: "Sue", last_name: "Jones")
			# offer = post.offers.new(user_id:userOffering.id, amount: 56)
			# offer.accepted = true
			# invalidOffer = post.offers.new(user_id:userOffering.id, amount: 60)

			# expect(invalidOffer).to be_invalid

		end

end


