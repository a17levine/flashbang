<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) + " | flashbang" : "flashbang" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% flash.each do |name, msg| -%>
      <script>
        flashbangAlerts = <%= flash.to_json.html_safe %>;
      </script>
    <% end -%>
    <% if current_user %>
      <%= render "shared/navbar" %>
    <% else %>
      <%= render "devise/shared/basic_navbar" %>
    <% end %>
    <div class="site-content">
      <%= yield %>  
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>